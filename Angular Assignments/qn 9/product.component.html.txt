<h2>Products</h2>
<ul>
  <li *ngFor="let p of products">
    {{p.title}} - ₹{{p.price}}
    <button (click)="addToSaveForLater(p)">Add to saveforlater</button>
  </li>
</ul>

<h2>Save For Later</h2>
<ul>
  <li *ngFor="let s of saveforlater">{{s.title}} - ₹{{s.price}}</li>
</ul>
